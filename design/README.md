# Design assets

Place Figma exports, screenshots, and token notes here (15 mobile frames at 390×844px).

**Priority hero frames:** 04 (documents library), 07 (chat empty), 08 (active chat).

When screenshots are added, the agent will:

1. Update `apps/web/styles/tokens.css`
2. Refine feature screens in `apps/web/features/`
3. Track progress in `docs/component-map.md`
