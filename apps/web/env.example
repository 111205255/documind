# Copy to apps/web/.env.local

NEXT_PUBLIC_SUPABASE_URL=https://vazxfcgbaawbreziszmy.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_anon_key

# FastAPI RAG backend (Step 4) — required for AI chat
NEXT_PUBLIC_API_URL=http://localhost:8000

# Server-only: Next.js /api/rag proxy sends this to the backend (optional)
RAG_API_URL=http://localhost:8000
RAG_API_KEY=

# E2E tests (optional) — authenticated Playwright flows
E2E_EMAIL=
E2E_PASSWORD=
