# Copy to apps/mobile/.env

EXPO_PUBLIC_SUPABASE_URL=https://vazxfcgbaawbreziszmy.supabase.co
EXPO_PUBLIC_SUPABASE_ANON_KEY=your_anon_key

# FastAPI RAG backend — use your PC IP for device testing (not localhost)
EXPO_PUBLIC_API_URL=http://localhost:8000

# Auth: mobile sends Supabase JWT in Authorization header (no client API key).
# Backend must have SUPABASE_JWT_SECRET + SUPABASE_URL + SUPABASE_SERVICE_ROLE_KEY set.
