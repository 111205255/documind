# DocuMind — Blueprint Step 4 / 6
services:
  backend:
    build: ./backend
    ports:
      - "8000:8000"
    env_file:
      - ./backend/.env
    volumes:
      - chroma_data:/app/chroma_data
    environment:
      - CHROMA_PERSIST_DIRECTORY=/app/chroma_data

volumes:
  chroma_data:
